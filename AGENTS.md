# Agent Notes

- Keep `README.md` up to date when changing user-facing behavior, setup steps, CLI commands or flags, environment variables, Docker usage, scheduling guidance, or operational workflows.
- Keep `AGENTS.md` up to date when adding, removing, or changing durable agent instructions for this repository.
- When a code change affects docs, update the docs in the same change rather than leaving follow-up cleanup work.
- If behavior changed and the correct documentation target is unclear, update both `README.md` and `AGENTS.md` if needed.
